[package]
name = "bevy_talks_editor"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.19", features = ["bevy_feathers", "system_clipboard"] }
bevy_talks = { path = "../.." }
